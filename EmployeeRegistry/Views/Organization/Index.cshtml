@model IEnumerable<EmployeeRegistry.Models.lutER_Organization>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.OrganizationalName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.OrganizationalName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OrganizationalId }) |
            @Html.ActionLink("Details", "Details", new { id=item.OrganizationalId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OrganizationalId })
        </td>
    </tr>
}

</table>
