@model IEnumerable<EmployeeRegistry.Models.lutER_Directorate>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DirectorateName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DirectorateName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DirectorateId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DirectorateId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DirectorateId })
        </td>
    </tr>
}

</table>
